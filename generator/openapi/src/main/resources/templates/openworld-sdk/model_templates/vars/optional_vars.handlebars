{{#if composedSchemas}}
{{#with composedSchemas}}
{{#each allOf}}
{{#unless complexType}}
{{#each vars}}
{{^required}}
    {{{name}}}: {{>model_templates/vars/vars_datatype}} = Field(default={{#if defaultValue}}{{{defaultValue}}}{{/if}}{{#unless defaultValue}}None{{/unless}})
{{/required}}
{{/each}}
{{/unless}}
{{/each}}
{{/with}}
{{/if}}
{{#unless composedSchemas}}
{{#each vars}}
{{^required}}
    {{{name}}}: {{>model_templates/vars/vars_datatype}} = Field(default={{#if defaultValue}}{{{defaultValue}}}{{/if}}{{#unless defaultValue}}None{{/unless}})
{{/required}}
{{/each}}
{{/unless}}
