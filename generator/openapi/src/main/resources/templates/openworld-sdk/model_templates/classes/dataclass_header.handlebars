{{#if composedSchemas}}
{{#with composedSchemas}}
{{#each allOf}}
{{#if complexType}}
class {{{classname}}}({{{complexType}}}):
{{/if}}
{{/each}}
{{/with}}
{{/if}}
{{#unless composedSchemas}}
class {{{classname}}}(BaseModel):
{{/unless}}
